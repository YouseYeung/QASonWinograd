P1: 介绍的WSC不够准确，应该明确指出WSC的难点在于获取背景知识和对背景知识进行的推理而不仅仅是代词消歧。
P2：解决WSC问题的方法有很多种，不可能就是一个理解句意和逻辑推理的问题，比方说有信息检索+数据统计方式，语义分析方式，机器学习方式。
P4：构建逻辑公式和逻辑推理并不等价，构建逻辑公式并不能作为一个子标题。 逻辑推理不等同于推理，特征向量相似度匹配不属于逻辑推理吧。这些内容都是需要去核实的。

P5：不能说 证明一届逻辑语言推理在解决WSC问题上的优越性，只能说，验证一下它是否有优越性。
P10：算法实现中的寻找谓词和寻找实体太经验性了，寻找一下是否有相关的文献资料。

实体取值范围受限规则改为封闭世界假定规则。

PPT每页只需要有一个标题就可以了。

总结对比所有的语义解析工具，Sempre是否是最好的解析工具，这方面是需要去了解的。

